**第一阶段：让一次 subagent 执行能够清楚地交接。**

**1. 收尾总结、运行编号、状态记录，是三个配合使用的功能。**

你的理解基本正确，但它们不必绑定实现：

- **收尾总结**：告诉主 Agent“做到哪了，还剩什么”。
- **`run_id`**：让主 Agent 能识别、查询这一次执行。
- **`status`**：让程序能判断它现在是否结束，以及为什么结束。

也就是说，**实现 30 轮后的总结，本身不依赖 `run_id`**；加编号是为了后面关联任务、查询结果和取消执行。

`run_id` 更准确的含义是“某个任务下的一次执行尝试”，不必再单独设计一个长期存在的子 Agent 身份。例如：

```
task_001：定位一个问题
  run_001：第一次执行，轮数耗尽
  run_002：根据第一次的发现重新执行
```

注意，第二次执行是否接续第一次的进度，取决于是否传入之前的摘要和必要证据，**共用 `task_id` 不会自动继承历史**。

关于状态记录：注释 1

| 状态               | 含义                                            |
| ------------------ | ----------------------------------------------- |
| `running`          | 正在执行                                        |
| `completed`        | 正常结束并提交结果，是否完成任务由主 Agent 验收 |
| `failed`           | 遇到无法继续的错误                              |
| `budget_exhausted` | 执行轮数等预算耗尽                              |
| `timed_out`        | 超过运行时间限制                                |
| `cancelled`        | 取消处理已经完成                                |

第一阶段还没实现取消时，可以暂时不使用 `cancelled`。

**状态由程序根据实际结束路径填写，不能让模型自行声明。**一次工具报错也不必马上设为 `failed`，因为子 Agent 可能根据错误调整操作并继续完成。

模型响应截断可以按你的想法，暂时不做专门处理，把它记为当前版本的已知边界即可。

**2. 结构化返回结果，你列的字段已经够用。**

初版可以就是：

```
run_id
task_id
status
summary       工作摘要
evidence      执行证据
remaining     剩余问题
```

再补一个可选的 `error`，存放无法继续时的具体错误原因，便于区分“模型请求失败”和“读取文件失败”等问题。

其中：

- `run_id`、`task_id`、`status`、`evidence`、`error` 由程序维护。
- `summary`、`remaining` 主要由模型根据执行记录总结。

**执行证据不需要一开始就做成庞大的日志系统。**可以在子 Agent 调用工具的位置，每次追加一条简单记录：

```
工具：edit_file
目标：stats.py
结果：成功

工具：bash
命令：python -m pytest tests/test_stats.py
退出码：1
输出摘要：……
```

你已经有统一的工具执行入口，shell 底层也已经取得退出码，可以从这些位置收集信息。

初版先记录“程序实际执行过什么”，不必马上实现完整文件差异追踪。但要如实描述：**记录到一次文件编辑成功，不等于已经完整捕获 shell 造成的所有文件变化。**

证据和日志也不需要存两套：保存一份工具执行记录，返回结果引用或摘取其中相关内容即可。

**3. 固定 10 分钟可以，但要明确它如何生效。**

你可以先固定两个限制：

```
最多执行 30 轮
最多执行 10 分钟
任一限制到达，就停止继续工作并收尾
```

关键是，**只在循环开头检查时间，无法保证正在等待的操作及时停止。**

例如第 9 分 59 秒启动一条耗时命令，如果它仍使用原来的 120 秒超时，就可能超过整次运行的 10 分钟限制。模型请求也存在同样的问题。

因此需要：

- 每轮模型调用前、每个工具执行前检查剩余时间。
- 等待模型和命令时，让它们的超时受剩余时间约束。
- 超时后停止后续操作，处理正在运行的命令，再生成交接结果。

另外，“10 分钟后再总结”意味着总结会额外花时间。可以先约定：**工作最多 10 分钟，再给一次短时间的收尾机会。**如果要求总时间严格不超过 10 分钟，就提前预留总结时间。

总结调用失败时，也应返回已有证据和明确状态，不能因为没有总结成功而丢掉整个结果。

**4. 你说的“工作卡”，需要和返回结果分开理解。**

它们最主要的区别是**使用时间和使用者不同**：

| 内容                       | 什么时候使用 | 主要给谁使用                 |
| -------------------------- | ------------ | ---------------------------- |
| 运行上下文，也就是“工作卡” | 执行过程中   | Python 程序                  |
| 结构化返回结果             | 执行结束后   | 主 Agent，以及查询结果的程序 |

运行上下文可以先包含：

```
run_id、task_id
工作目录
允许使用的工具或权限模式
最大轮数、截止时间
取消信号
```

例如：

> 执行 `write_file` 之前，程序查工作卡，发现这次运行只允许读取，于是拒绝执行。

返回结果则是：

> 这次运行因超时结束，已经检查两个文件，发现一个可能原因，尚未验证。

**工作卡控制这次执行，返回结果描述这次执行。**两者可以共用编号，也可以放在同一个运行管理对象里，但职责不同。

------

**第二阶段：让最多两个 subagent 在后台执行，并能查询和停止。**

**1. 参考后台命令的管理方式，这个方向正确。**

可以写一个管理类，负责：

```
启动运行 → 返回 run_id
保存每次运行的状态和结果
限制同时最多运行两个
查询、等待、取消指定运行
收集已结束运行的通知
```

类负责执行管理，主 Agent 仍负责判断“要不要委派、委派什么、如何使用结果”。

主 Agent 可以通过工具查询状态，主循环也可以在每轮开始时收集完成通知。等待期间不需要反复调用 LLM 问“好了没有”，可以让程序等待一段时间，结果到达后再交给模型处理。

已完成结果应该继续按 `run_id` 保存。**收集过一次通知，不等于删除这次运行的结果。**

**2. “控制 SUB_TOOLS”只完成了只读限制的一部分。**

如果你的意思是：

> 子 Agent 只保留读取文件、匹配文件和受限制的文本搜索工具。

那么这是合适的初版方向。

如果只是删除写入和编辑工具，仍保留任意 `bash`，就无法保证只读，因为 shell 可以执行写文件的脚本。

还要区分：

- `SUB_TOOLS`：告诉模型可以请求哪些工具。
- 工具执行层：检查收到的请求是否真的允许执行。

**只读运行的实际工具处理函数也应该只开放允许的操作。**不能只缩小模型看到的菜单，却继续使用包含写入能力的完整执行映射。

**3. 取消应先发出停止要求，完成处理后再标记已取消。**

你说的“改状态、清理、总结”需要稍微调整顺序：

```
主 Agent 请求取消
        ↓
设置取消信号
        ↓
子循环不再启动新操作
        ↓
终止或等待当前操作，并清理资源
        ↓
保存已有记录，尽可能生成摘要
        ↓
状态设为 cancelled，返回结果
```

不能收到取消请求就立即标成 `cancelled`，但实际命令仍在执行。必要时可以增加一个中间状态 `cancelling`，表示“正在停止”。

清理也只能针对该 `run_id` 的资源，不能取消 A 时把 B 的命令一起停止。

取消后的模型总结可以是有短超时的收尾步骤；若希望立即取消、停止继续调用模型，就直接返回已有执行记录。**有没有生成漂亮的总结，不应该影响取消是否成功。**

**4. 后台结果到达后再调用 LLM，你的理解正确，但应有条件。**

准确的规则是：

> 主 Agent 准备结束时，如果收到了尚未处理的后台结果，就把结果注入 `messages`，然后继续主循环。

这次模型调用不一定只做总结，也可能发现需要继续读文件、修改代码或执行验证。

如果没有新结果，也没有需要等待的子 Agent，就正常结束，避免无条件多调用一次模型。

------

**第三阶段：实际测试，再整理为别人能复现的项目。**

你说的实际测试是核心，至少要覆盖：

- 正常结束，主 Agent 能根据证据验收。
- 工具报错后可以恢复，或明确失败。
- 轮数耗尽、时间耗尽后保留交接结果。
- 两个子 Agent 中一个失败，另一个结果仍然完整。
- 只读运行无法执行写入操作。
- 取消后不再启动新工具。
- 后台结果回来后，主 Agent 确实继续处理。

此外，还需要依赖清单、配置示例、README 和固定演示任务，让别人能运行这些案例。

**实际实施时，我建议先做一个最小闭环：给同步 subagent 加运行编号和状态，记录工具执行结果，并让正常结束、轮数耗尽和异常退出都经过统一的收尾入口。**然后补时间限制和运行上下文，验证这些路径后，再加入并发、只读和取消。